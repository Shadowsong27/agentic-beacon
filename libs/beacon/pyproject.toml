[project]
name = "agentic-beacon"
version = "3.6.0"
description = "Agentic Beacon CLI - Distribute knowledge contexts and skills for AI-assisted development teams"
readme = "README.md"
# Upper bound excludes 3.14 deliberately: a transitive Click 8.4.0
# stdlib-driven change in Click's "no such option" error format (released
# on 2026-05-17 alongside Python 3.14 becoming the ubuntu-latest default)
# is incompatible with the test assertions in this codebase. CI matrix in
# .github/workflows/ci.yml tests {3.12, 3.13} — pyproject metadata now
# matches what is actually tested. Bump deliberately when the 3.14
# compatibility sweep lands.
requires-python = ">=3.12,<3.14"
license = { text = "MIT" }
authors = [
    { name = "Agentic Beacon Contributors" }
]
keywords = ["ai", "agents", "agentic", "context", "knowledge-management", "developer-tools", "beacon"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Software Development :: Documentation",
    "Topic :: Software Development :: Quality Assurance",
]
dependencies = [
    "click>=8.1.0",
    "rich>=13.0.0",
    "pyyaml>=6.0.0",
    "loguru>=0.7.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "textual>=0.80.0",
]

[project.urls]
Homepage = "https://github.com/Shadowsong27/agentic-beacon"
Documentation = "https://github.com/Shadowsong27/agentic-beacon/tree/main/libs/beacon"
Repository = "https://github.com/Shadowsong27/agentic-beacon"
Issues = "https://github.com/Shadowsong27/agentic-beacon/issues"

[project.scripts]
abc = "beacon.cli.main:main"

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "pytest-asyncio>=0.21.0",
    "build>=1.0.0",
    "twine>=4.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/beacon"]
