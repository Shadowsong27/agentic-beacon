# beacon.yaml for a TypeScript project
# Copy to your project's .agentic-beacon/beacon.yaml

artifacts:
  skills:
    - skills/code-review/
  contexts:
    - contexts/typescript.md
    - contexts/global.md
